// C O N D I T I O N A L S


// comparison operator '==' check for VALUE likeness: 3 == 7 returns false
// '===' checks for VALUE and TYPE likeness: 3 = "3" returns false

// one can only use a SWITCH statement if you have one variable.
// Rule of thumb, only use a switch if you have three or more conditions to check

if (condition) {
    result = 'doIfTrue';
} else {
    result = 'doIfFalse';
}

// TERNARY operator (a one-line XXX)
let result = (condition) ? 'doIfTrue' : 'doIfFalse'